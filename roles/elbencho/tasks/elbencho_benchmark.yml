#SPDX-License-Identifier: MIT-0
---
# DEPRECATED location — this file used to be a full playbook under tasks/.
# Use: ansible-playbook -i inventory.ini playbooks/elbencho.yml --tags <tag>
#
# Kept so old references fail loudly with a message instead of silent no-op.
- name: Elbencho tasks moved
  ansible.builtin.fail:
    msg: >-
      Elbencho orchestration moved to playbooks/elbencho.yml.
      Run: ansible-playbook -i inventory.ini playbooks/elbencho.yml --tags <tag>
  tags: always
